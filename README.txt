Pupil pilot bundle — built 2026-09-03T20:46:57.166Z

Host this folder on any HTTPS static host (GitHub Pages: put these files in a public
repository, Settings -> Pages -> deploy from branch). Send participants the URL.
Camera access requires HTTPS, so opening index.html directly from disk will not work.

Results: each participant downloads a JSON file at the end and sends it back manually.
Nothing is uploaded by the page itself.
